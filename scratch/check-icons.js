import * as Lucide from 'lucide-react';
console.log('Available icons count:', Object.keys(Lucide).length);
console.log('Is Clock available?', 'Clock' in Lucide);
console.log('Sample exports:', Object.keys(Lucide).slice(0, 10));
